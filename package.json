{
  "name": "abxr-editor",
  "version": "1.0.0",
  "description": "your assets editor",
  "scripts": {
    "build": "yarn install",
    "start": "http-server -p 3000"
  },
  "devDependencies": {
    "http-server": "^0.12.3"
  },
  "dependencies": {
    "three": "^0.154.0"
  }
}
